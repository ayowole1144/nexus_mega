document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('header');const headerHeight=header.offsetHeight;const body=document.body;window.addEventListener('scroll',()=>{if(window.scrollY>headerHeight){header.classList.add('scrolled');body.style.paddingTop=`${ headerHeight }px`}else{header.classList.remove('scrolled');body.style.paddingTop='0'}});const mobileMenuIcon=document.querySelector('.mobile-menu-icon');const mainNav=document.querySelector('.main-nav');if(mobileMenuIcon&&mainNav){mobileMenuIcon.addEventListener('click',()=>{mainNav.classList.toggle('active')})}const slides=document.querySelectorAll('.hero-slider .slide');let currentSlide=0;const showSlide=(n)=>{slides.forEach(slide=>slide.classList.remove('active'));slides[n].classList.add('active')};const nextSlide=()=>{currentSlide=(currentSlide+1)%slides.length;showSlide(currentSlide)};if(slides.length>1){setInterval(nextSlide,5000)}showSlide(currentSlide);const counterItems=document.querySelectorAll('.counter');const options={root:null,rootMargin:'0px',threshold:0.5};const animateCounter=(entries,observer)=>{entries.forEach(entry=>{if(entry.isIntersecting){const counter=entry.target;const target=parseInt(counter.getAttribute('data-target'));let count=0;const increment=target/200;const updateCount=()=>{if(count<target){count+=increment;counter.innerText=Math.ceil(count);requestAnimationFrame(updateCount)}else{counter.innerText=target}};updateCount();observer.unobserve(counter)}})};const observer=new IntersectionObserver(animateCounter,options);counterItems.forEach(item=>{observer.observe(item)});const projectCarousels=document.querySelectorAll('.project-carousel');projectCarousels.forEach(carousel=>{const slides=carousel.querySelector('.project-carousel-slides');const prevBtn=carousel.querySelector('.project-carousel-prev');const nextBtn=carousel.querySelector('.project-carousel-next');const indicatorsContainer=carousel.querySelector('.project-carousel-indicators');const allSlides=carousel.querySelectorAll('.project-carousel-slides img');let currentSlide=0;const showSlide=(index)=>{slides.style.transform=`translateX(-${index*100}%)`;updateIndicators(index)};const nextSlide=()=>{currentSlide=(currentSlide+1)%allSlides.length;showSlide(currentSlide)};const prevSlide=()=>{currentSlide=(currentSlide-1+allSlides.length)%allSlides.length;showSlide(currentSlide)};const updateIndicators=(index)=>{indicatorsContainer.innerHTML='';allSlides.forEach((slide,i)=>{const indicator=document.createElement('span');if(i===index){indicator.classList.add('active')}indicator.addEventListener('click',()=>{currentSlide=i;showSlide(currentSlide)});indicatorsContainer.appendChild(indicator)})};if(allSlides.length>1){prevBtn.addEventListener('click',prevSlide);nextBtn.addEventListener('click',nextSlide);updateIndicators(0);let autoSlideInterval=setInterval(nextSlide,5000);carousel.addEventListener('mouseenter',()=>clearInterval(autoSlideInterval));carousel.addEventListener('mouseleave',()=>{autoSlideInterval=setInterval(nextSlide,5000)})}else{prevBtn.style.display='none';nextBtn.style.display='none'}});const contactForm=document.getElementById('contactForm');const formMessage=document.getElementById('form-message');contactForm.addEventListener('submit',function(event){event.preventDefault();const name=contactForm.elements['name'].value;const email=contactForm.elements['email'].value;const message=contactForm.elements['message'].value;if(name.trim()===''||email.trim()===''||message.trim()===''){formMessage.innerText='Please fill out all fields.';formMessage.style.color='#ff4d4d';return}console.log('Form submitted:',{name,email,message});formMessage.innerText='Thank you for your message! We will get back to you shortly.';formMessage.style.color='#00bf63';setTimeout(()=>{contactForm.reset();formMessage.innerText=''},5000)})});